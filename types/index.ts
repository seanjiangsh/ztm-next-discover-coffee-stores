export type MapboxType = {
  id: string;
  properties: { name: string; full_address: string };
};

export type CoffeeStoreType = {
  id: string;
  name: string;
  imgUrl: string;
  address: string;
};
